fn main() {
    let name = "Pluto"
    let version = 1
    print(f"Welcome to {name} v{version}!")

    let x = 10
    let y = 20
    print(f"{x} + {y} = {x + y}")

    let pi = 3.14
    print(f"pi is approximately {pi}")

    let is_ready = true
    print(f"system ready: {is_ready}")
}
