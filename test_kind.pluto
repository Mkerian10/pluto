class Person {
    name: string
    age: int
    active: bool
}

enum Status {
    Active
    Inactive { reason: string }
}

fn main() {
    // Test class reflection
    let kind = TypeInfo::kind<Person>()
    print("Testing class reflection...")
    print("Kind type check...")
    
    // For now, just test that we can call it and get a result
    // Full match expression support may need separate testing
    print("TypeInfo::kind<Person>() completed successfully")
    
    // Test enum reflection  
    let enum_kind = TypeInfo::kind<Status>()
    print("TypeInfo::kind<Status>() completed successfully")
    
    print("All reflection tests passed!")
}
