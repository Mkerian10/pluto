[package]
name = "pluto-sdk"
version = "0.1.0"
edition = "2021"

[dependencies]
pluto = { path = ".." }
uuid = { version = "1", features = ["v4", "serde"] }
thiserror = "2"

[dev-dependencies]
tempfile = "3"
